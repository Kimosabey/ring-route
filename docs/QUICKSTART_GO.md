
### 🚀 Running the High-Performance Backend (Go)

This project uses a high-concurrency **Go (Fiber)** backend by default.

1.  **Install Go**: Ensure Go 1.21+ is installed.
2.  **Navigate to directory**: `cd backend-go`
3.  **Run Engine**:
    ```bash
    go run main.go
    ```
    *Server listens on port 3002*

### 🎨 Running the Frontend

1.  **Navigate to directory**: `cd frontend`
2.  **Dev Server**:
    ```bash
    npm run dev
    ```
    *Frontend available at http://localhost:3000*
